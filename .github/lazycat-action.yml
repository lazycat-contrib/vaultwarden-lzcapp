version: 1

project:
  root: .
  build_config: lzc-build.yml
  package_file: package.yml
  output: dist/vaultwarden.lpk

update:
  strategy: publish
  version_source:
    type: image
    image: vaultwarden

build:
  run_buildscript: false

images:
  - id: vaultwarden
    target: service
    service: vaultwarden
    source: docker.io/vaultwarden/server
    channel: stable
    tag_regex: '^v?\d+\.\d+\.\d+$'
    version_regex: '^v?(?P<version>\d+\.\d+\.\d+)$'
    delivery:
      mode: lazycat

stores:
  official:
    enabled: true
    skip_if_version_exists: true
    create_if_missing: false
    changelog_locales: [zh, en]
  private:
    enabled: true
    skip_if_version_exists: true
    name: Vaultwarden+
    summary: Published from GitHub Actions
